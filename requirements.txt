# 주식 브리핑 사이트 - Python 의존성 목록
# -------------------------------------------------------------
# 설치:   pip install -r requirements.txt
# 업데이트: pip install -U -r requirements.txt
#
# 버전은 ">=" 로 느슨하게 고정 — 보안 패치 / 마이너 업데이트는 자동으로 받되,
# 메이저 호환성 깨짐은 동작 확인 후 수동으로 올린다.
# =============================================================

# Anthropic Claude API 공식 Python SDK — 브리핑 텍스트 생성용
anthropic>=0.40.0

# .env 파일에서 환경 변수 로드 — 로컬 개발 시 API 키 주입용
python-dotenv>=1.0.0

# 한국 주식 시장 데이터 수집 — KRX 종가/거래량/등락률 등을 가져옴
finance-datareader>=0.9.50

# 콘솔용 표 출력 — 한글(CJK) 폭 자동 처리로 정렬 안 깨짐
tabulate>=0.9.0

# 동아시아 문자(한/중/일) 폭 계산 — tabulate 가 이 라이브러리를 발견하면 자동으로 사용
wcwidth>=0.2.0
